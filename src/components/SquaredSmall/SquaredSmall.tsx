import React from "react";
import { IconWrapper } from "./SquaredSmallStyled";

export const SquaredSmall = ({icon}) => {
    return (
        <IconWrapper>{icon}</IconWrapper>
    );
}

export default SquaredSmall;

